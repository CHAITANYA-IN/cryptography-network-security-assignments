Make sure to install following packages before running the program:
  numpy
  sympy

Command to install these packages:
pip install numpy
pip install sympy

Run the following command:
python3 hill-cipher.py